name: Build and Push Optimized Docker Images

on:
  push:
    branches:
      - main
    paths:
      - 'docker/Dockerfile.document.optimized'
      - 'docker/Dockerfile.sheets.optimized'
      - 'docker/Dockerfile.presentation.optimized'
      - 'docker/scripts/**'
      - 'docker/branding/**'
      - '.github/workflows/ghcr-build-optimized.yml'
  workflow_dispatch:
    inputs:
      image:
        description: 'Which image to build'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - document
          - sheets
          - presentation

env:
  REGISTRY: ghcr.io

jobs:
  build-document:
    if: github.event_name == 'push' || inputs.image == 'all' || inputs.image == 'document'
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo rm -rf /opt/hostedtoolcache/go
          sudo rm -rf /opt/hostedtoolcache/node
          sudo docker image prune --all --force
          sudo apt-get clean
          echo "Disk space after cleanup:"
          df -h

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push TeamSync Document (Optimized)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.document.optimized
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-document:optimized
          cache-from: type=gha,scope=document-optimized
          cache-to: type=gha,mode=max,scope=document-optimized
          platforms: linux/amd64
          provenance: false

      - name: Report image size
        run: |
          echo "### TeamSync Document (Optimized)" >> $GITHUB_STEP_SUMMARY
          docker pull ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-document:optimized
          SIZE=$(docker images ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-document:optimized --format "{{.Size}}")
          echo "Image size: **$SIZE**" >> $GITHUB_STEP_SUMMARY
          echo "::notice title=TeamSync Document Optimized Size::$SIZE"

  build-sheets:
    if: github.event_name == 'push' || inputs.image == 'all' || inputs.image == 'sheets'
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo rm -rf /opt/hostedtoolcache/go
          sudo rm -rf /opt/hostedtoolcache/node
          sudo docker image prune --all --force
          sudo apt-get clean
          echo "Disk space after cleanup:"
          df -h

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push TeamSync Sheets (Optimized)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.sheets.optimized
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-sheets:optimized
          cache-from: type=gha,scope=sheets-optimized
          cache-to: type=gha,mode=max,scope=sheets-optimized
          platforms: linux/amd64
          provenance: false

      - name: Report image size
        run: |
          echo "### TeamSync Sheets (Optimized)" >> $GITHUB_STEP_SUMMARY
          docker pull ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-sheets:optimized
          SIZE=$(docker images ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-sheets:optimized --format "{{.Size}}")
          echo "Image size: **$SIZE**" >> $GITHUB_STEP_SUMMARY
          echo "::notice title=TeamSync Sheets Optimized Size::$SIZE"

  build-presentation:
    if: github.event_name == 'push' || inputs.image == 'all' || inputs.image == 'presentation'
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo rm -rf /opt/hostedtoolcache/go
          sudo rm -rf /opt/hostedtoolcache/node
          sudo docker image prune --all --force
          sudo apt-get clean
          echo "Disk space after cleanup:"
          df -h

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push TeamSync Presentation (Optimized)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.presentation.optimized
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-presentation:optimized
          cache-from: type=gha,scope=presentation-optimized
          cache-to: type=gha,mode=max,scope=presentation-optimized
          platforms: linux/amd64
          provenance: false

      - name: Report image size
        run: |
          echo "### TeamSync Presentation (Optimized)" >> $GITHUB_STEP_SUMMARY
          docker pull ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-presentation:optimized
          SIZE=$(docker images ${{ env.REGISTRY }}/${{ github.repository_owner }}/teamsync-presentation:optimized --format "{{.Size}}")
          echo "Image size: **$SIZE**" >> $GITHUB_STEP_SUMMARY
          echo "::notice title=TeamSync Presentation Optimized Size::$SIZE"
